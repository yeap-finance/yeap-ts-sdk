fragment BorrowRiskParametersFields on borrow_risk_parameters_current {
  brw
  collateral
  market
  vault
  vault_info {
    ...VaultInfoFields
  }
  borrow_setting {
    borrow_cap
    borrow_permission
  }
}
